# Local visual checks

Preview images are generated locally from the player's own imported game.
The repository and release ZIP contain no game screenshots. Retained capture
drivers under `tests/` exercise the Bag, Party, PC, Pokédex and battle screens.
Write captures outside the repository, or to the ignored `screenshots/` folder.
Do not commit or package the captured images.
